[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run db:generate",
  "npm run build"
]

[start]
cmd = "npm run db:migrate:deploy && npm start"

[variables]
NODE_ENV = "production"
